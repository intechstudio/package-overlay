{
  "name": "package-overlay",
  "version": "0.0.2",
  "description": "Overlay",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write .",
    "install:components": "cd ./components && npm install",
    "build:components": "cd ./components && npm run build",
    "dev:components": "cd ./components && npm run dev",
    "postinstall": "run-p install:*",
    "build": "run-p build:*",
    "dev": "run-p dev:*"
  },
  "author": "",
  "license": "ISC",
  "grid_editor": {
    "componentsPath": "components/dist/components.js",
    "preferenceComponent": "overlay-preference",
    "shortDescription": "Show extra information on computer screen.",
    "mainIcon": "overlay-main-logo.svg",
    "menuIcon": "overlay-white-icon.svg"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "prettier-plugin-svelte": "^3.3.3",
    "npm-run-all": "^4.1.5"
  }
}
